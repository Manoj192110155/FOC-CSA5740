#include<stdio.h>
int main() {
    int n;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    if(n>18){
        printf("eligible to vote");
    }
    else{
        printf("not eligible");
    }
}

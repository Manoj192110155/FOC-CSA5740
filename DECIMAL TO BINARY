#include <stdio.h>
int main() {
    int a;
    printf("Enter a decimal number: ");
    scanf("%d", &a);
    for (int i = 8; i >= 0; i--) {
        int bit = (a >> i) & 1;
        printf("%d", bit);
    }
    return 0;
}

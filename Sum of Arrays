#include <stdio.h>
#include <string.h>
int main() {
    int arr[100],x,i,sum=0;
    printf("enter the size of the arr:");
    scanf("%d",&x);
    printf("enter the elements:\n");
    for(i=0;i<x;i++){
        scanf("%d",&arr[i]);
        sum+=arr[i];
    }
    printf("total=%d",sum);
}
